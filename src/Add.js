import React from "react";

function Add() {
  var addStyles = {
    marginLeft: 20,
    marginRight: 20,
    fontSize: 30,
    color: "Blue",
    cursor: "pointer"
  };
  return <i style={addStyles} className="fa fa-plus-square" />;
}

export default Add;
